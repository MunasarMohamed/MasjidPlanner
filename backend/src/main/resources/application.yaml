spring:
  datasource:
    url: jdbc:postgresql://cawzrgxqfcoomyoitgjv.supabase.co:6543/postgres?sslmode=require
    driver-class-name: org.postgresql.Driver
    username: postgres
   
  jpa:
    hibernate:
      ddl-auto: update  # Use 'update' not 'create-drop' for production!
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect  # ✅ PostgreSQL dialect
        format_sql: true
  
  # Remove H2 section completely - you're not using H2!
  
server:
  port: 8080

jwt:
  secret: "5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437"
  expiration: 86400000

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type: TRACE