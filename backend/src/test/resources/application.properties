# Use an in-memory H2 database for tests to avoid requiring a real Postgres instance
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
logging.level.org.hibernate.SQL=DEBUG

# Provide required placeholders used by app during tests
security.jwt.secret-key=Zm9vYmFyYmF6MTIzNDU2Nzg5MDEyMzQ1Njc4OTAxMjM0
security.jwt.expiration-time=3600000
